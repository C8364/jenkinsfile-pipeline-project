pipeline {
    agent any
    stages {
        stage('run') {
            steps {
                echo 'Clarusway_Way to Reinvent FERDI'
                sh 'python --version'
                sh 'python pipeline.py'
            }
        }
    }
}
